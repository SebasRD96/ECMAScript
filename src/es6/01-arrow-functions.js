
//Función normal
function square(num) {
    return num * num;
}

// Función con arrow
const square = (num) => {
    return num * num;
}
// Función con arrow simplificada
const square = num => num * num ;